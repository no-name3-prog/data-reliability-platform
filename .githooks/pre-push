#!/usr/bin/env bash
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
if ! command -v docker >/dev/null 2>&1; then
  echo "pre-push: docker is required" >&2
  exit 1
fi
echo "==> pre-push: nextest (container)"
make test
echo "pre-push: OK"
